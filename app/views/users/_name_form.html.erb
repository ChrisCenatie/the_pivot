<div class="well">
  <%= form_for :user_and_address, url: user_path, method: :patch do |f| %>
    <div class="form-group">
      <%= f.label "First Name" %>
      <%= f.text_field :first_name, class: "form-control input-md", placeholder: "#{current_user.first_name}" %>
    </div>
    <div class="form-group">
      <%= f.label "First Name" %>
      <%= f.text_field :last_name, class: "form-control input-md", placeholder: "#{current_user.last_name}" %>
    </div>
    <%= f.submit "Update Name", class: "btn btn-primary" %>
  <% end %>
</div>
