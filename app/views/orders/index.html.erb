<% @orders.each do |order| %>
  <strong><%= link_to "Order #{order.id}", order_path(order) %></strong>
  <strong> Created at:</strong>  <%= order.created_at %>
<strong> Total Price:</strong> <%= order.total_price %>
<% end %>
